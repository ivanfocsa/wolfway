---
import { mainNav } from '@/content/navigation';
import Button from '@/components/Button.astro';
---

<header id="site-header" class="fixed top-0 left-0 z-50 w-full bg-[#F5F7FA]/95 backdrop-blur-md border-b border-slate-200/50 py-3 transition-all duration-300 select-none">
  <nav class="mx-auto max-w-7xl px-5 lg:px-10 flex items-center justify-between">
    <!-- Logo & Brand Name -->
    <a href="/" class="flex items-center gap-2.5 group" aria-label="WolfWay Home">
      <img
        id="header-logo"
        src="/images/logo_icon.png"
        alt="WolfWay Logo"
        class="h-11 w-11 object-contain transition-transform duration-300 group-hover:scale-105"
      />
      <span
        id="header-title"
        class="font-heading text-lg sm:text-xl font-extrabold uppercase tracking-widest text-ink group-hover:text-cyan transition-colors duration-300"
      >
        WolfWay
      </span>
    </a>

    <!-- Desktop Nav Links & CTA -->
    <div class="hidden lg:flex items-center gap-8">
      <div class="flex items-center gap-5">
        {mainNav.map((item) => (
          <a
            href={item.href}
            class="text-xs font-semibold uppercase tracking-wider text-slate-500 hover:text-ink px-2 py-1 transition-colors duration-300 relative group/link"
          >
            {item.label}
            <span class="absolute bottom-0 left-2 right-2 h-[2px] bg-cyan scale-x-0 group-hover/link:scale-x-100 transition-transform duration-300 origin-left" aria-hidden="true"></span>
          </a>
        ))}
      </div>
      <Button href="/request-a-quote" variant="primary" size="sm" class="rounded-sm font-heading tracking-wider uppercase">
        Request a Quote
      </Button>
    </div>

    <!-- Mobile Menu Open Toggle -->
    <button
      id="mobile-menu-open"
      class="relative z-50 flex h-10 w-10 items-center justify-center lg:hidden rounded-sm bg-slate-100 border border-slate-200 hover:bg-slate-200/60 active:scale-95 transition-all duration-200"
      aria-label="Open menu"
      aria-expanded="false"
      aria-controls="mobile-menu"
    >
      <span class="sr-only">Open menu</span>
      <div class="flex flex-col gap-[5px]">
        <span class="block h-[2px] w-[18px] bg-ink rounded-full"></span>
        <span class="block h-[2px] w-[12px] bg-ink/70 rounded-full self-end"></span>
        <span class="block h-[2px] w-[18px] bg-ink rounded-full"></span>
      </div>
    </button>
  </nav>
</header>

<!-- Mobile menu overlay -->
<div
  id="mobile-menu-overlay"
  class="fixed inset-0 z-[60] lg:hidden"
  aria-hidden="true"
  style="pointer-events:none;"
>
  <!-- Backdrop -->
  <div
    id="menu-backdrop"
    class="absolute inset-0 bg-navy/60 backdrop-blur-sm"
    style="opacity:0; transition: opacity 0.4s ease;"
  ></div>

  <!-- Panel — slides in from the right -->
  <div
    id="menu-panel"
    class="absolute top-0 right-0 h-full w-[82vw] max-w-xs flex flex-col"
    style="
      background: #111722;
      border-left: 1px solid rgba(255,255,255,0.06);
      transform: translateX(100%);
      transition: transform 0.45s cubic-bezier(0.16, 1, 0.3, 1);
      box-shadow: -20px 0 60px rgba(0,0,0,0.5);
    "
  >
    <!-- Panel header: logo + close button -->
    <div class="flex items-center justify-between px-6 pt-5 pb-4 border-b border-white/5">
      <a href="/" class="flex items-center gap-2.5" id="menu-logo-link" aria-label="Home">
        <img src="/images/logo_icon.png" alt="WolfWay" class="h-10 w-10 object-contain" />
        <span class="font-heading text-base font-extrabold uppercase tracking-widest text-white">WolfWay</span>
      </a>

      <!-- Close button -->
      <button
        id="mobile-menu-close"
        class="flex h-10 w-10 items-center justify-center rounded-sm bg-white/5 border border-white/10 hover:bg-red-500/10 hover:border-red-400/30 active:scale-95 transition-all duration-200 group"
        aria-label="Close menu"
      >
        <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
          <path d="M2 2l14 14M16 2L2 16" stroke="white" stroke-width="2" stroke-linecap="round" class="group-hover:stroke-red-400 transition-colors duration-200"/>
        </svg>
      </button>
    </div>

    <!-- Nav links -->
    <nav class="flex-1 flex flex-col justify-center px-6 py-4 gap-0.5" aria-label="Mobile navigation">
      {mainNav.map((item, i) => (
        <a
          href={item.href}
          class="mobile-nav-link group flex items-center justify-between py-4 border-b border-white/5 last:border-0"
          style={`--i: ${i};`}
        >
          <span class="font-heading text-lg font-bold uppercase tracking-wider text-white/70 group-hover:text-white group-active:text-cyan transition-colors duration-200">
            {item.label}
          </span>
          <svg class="h-4 w-4 text-white/20 group-hover:text-cyan group-hover:translate-x-1 transition-all duration-200" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
      ))}
    </nav>

    <!-- CTA + Contact -->
    <div class="px-6 pb-8 pt-4 border-t border-white/5 space-y-4">
      <Button
        id="menu-cta"
        href="/request-a-quote"
        variant="primary"
        size="lg"
        arrow={true}
        class="w-full justify-center rounded-sm"
      >
        Request a Quote
      </Button>

      <div class="text-center pt-2">
        <p class="text-[10px] uppercase tracking-[0.2em] text-white/25 font-semibold mb-1.5">Call us anytime</p>
        <a href="tel:+12244464256" class="text-base font-heading font-bold text-white/80 hover:text-cyan transition-colors">
          +1 (224) 446-4256
        </a>
      </div>
    </div>
  </div>
</div>

<script>
  /* ── Elements ── */
  const openBtn  = document.getElementById('mobile-menu-open')  as HTMLButtonElement | null;
  const closeBtn = document.getElementById('mobile-menu-close') as HTMLButtonElement | null;
  const overlay  = document.getElementById('mobile-menu-overlay') as HTMLElement | null;
  const backdrop = document.getElementById('menu-backdrop')     as HTMLElement | null;
  const panel    = document.getElementById('menu-panel')        as HTMLElement | null;

  let menuOpen = false;

  /* ── Open ── */
  function openMenu() {
    menuOpen = true;
    if (overlay) overlay.style.pointerEvents = 'auto';
    overlay?.setAttribute('aria-hidden','false');
    openBtn?.setAttribute('aria-expanded','true');
    if (backdrop) backdrop.style.opacity = '1';
    if (panel)    panel.style.transform  = 'translateX(0)';
    document.body.style.overflow = 'hidden';
  }

  /* ── Close ── */
  function closeMenu() {
    menuOpen = false;
    if (backdrop) backdrop.style.opacity = '0';
    if (panel)    panel.style.transform  = 'translateX(100%)';
    openBtn?.setAttribute('aria-expanded','false');
    overlay?.setAttribute('aria-hidden','true');
    setTimeout(() => {
      if (!menuOpen && overlay) overlay.style.pointerEvents = 'none';
    }, 450);
    document.body.style.overflow = '';
  }

  /* ── Bind ── */
  openBtn?.addEventListener('click',  openMenu);
  closeBtn?.addEventListener('click', closeMenu);
  backdrop?.addEventListener('click', closeMenu);

  overlay?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', closeMenu);
  });

  document.addEventListener('keydown', e => {
    if (e.key === 'Escape' && menuOpen) closeMenu();
  });
</script>

<style>
  /* Staggered nav links when panel opens */
  @keyframes linkIn {
    from { opacity: 0; transform: translateX(20px); }
    to   { opacity: 1; transform: translateX(0); }
  }
  #menu-panel[style*="translateX(0)"] .mobile-nav-link {
    animation: linkIn 0.4s cubic-bezier(0.16,1,0.3,1) both;
    animation-delay: calc(var(--i) * 55ms + 200ms);
  }

  /* CTA fade-in */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(10px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  #menu-panel[style*="translateX(0)"] #menu-cta {
    animation: fadeUp 0.45s cubic-bezier(0.16,1,0.3,1) both;
    animation-delay: 420ms;
  }
</style>
